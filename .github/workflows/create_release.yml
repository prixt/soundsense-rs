name: Create Release

on:
  push:
    tags: 'v[0-9]+.[0-9]+.[0-9]+**'

jobs:
  create-release:
    if: "!contains(github.event.head_commit.message, 'ci-skip')"
    runs-on: windows-latest
    name: Prepare Release
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: $GITHUB_REF
          release_name: $GITHUB_REF
          draft: false
          prerelease: ${{contains( $GITHUB_REF, 'alpha' ) || contains( $GITHUB_REF, 'beta' )}}